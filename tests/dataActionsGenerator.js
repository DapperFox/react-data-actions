import expect from 'expect';
import toQueryString from '../../helpers/toQueryString';

describe('dataActionsGenerator', function () {
  it('hol', function () {
    expect(toQueryString()).toEqual('');
  });
});
